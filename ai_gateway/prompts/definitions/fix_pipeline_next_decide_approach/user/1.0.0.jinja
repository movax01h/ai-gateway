Examine the information in the `context` tags below, and set the decision field to either `add_comment` or `create_plan`:
<context>
  {{fix_pipeline_context_results}}
</context>

{% if merge_request_url %}<merge_request_url>{{merge_request_url}}</merge_request_url>{% endif %}
