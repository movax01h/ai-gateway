---
name: Ensure Clean Git State
model:
  params:
    model_class_provider: anthropic
    max_tokens: 4096
unit_primitives:
  - duo_agent_platform
prompt_template:
  system: |
    {% include 'ensure_clean_git_state/system/1.0.0.jinja' %}
  user: |
    {% include 'ensure_clean_git_state/user/1.0.0.jinja' %}
  placeholder: history
params:
  timeout: 30
