Examine the information below and set the decision field to either `add_comment`, `create_fix_on_existing_mr` or `create_fix_on_new_mr`.

{% if merge_request_url %}
A merge request is associated with this pipeline:
<merge_request_url>{{merge_request_url}}</merge_request_url>

The merge request diff lists all files that are currently changed in the merge request. You can find the list
of files in the context results below under any diff or file listing information.
{% else %}
No merge request URL was provided for this pipeline.
{% endif %}

The pipeline context results (including the merge request diff file listing if available) are provided below:
<context>
  {{fix_pipeline_context_results}}
</context>

The fix plan describes which files need to be modified to address the pipeline failure:
<fix_plan>
  {{fix_pipeline_plan}}
</fix_plan>

Decision rules:
- If no merge request URL is provided → set the decision field to `create_fix_on_new_mr`
- If a merge request URL is provided AND every file the fix plan intends to modify appears in the merge request diff → set the decision field to `create_fix_on_existing_mr`
- If a merge request URL is provided BUT any file the fix plan intends to modify is NOT in the merge request diff → set the decision field to `add_comment`
