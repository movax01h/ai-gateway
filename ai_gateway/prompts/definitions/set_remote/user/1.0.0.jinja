GitLab Project details:
<project>
    repository_url: {{ repository_url }}
</project>
Prepare the required `run_git_command` tool call.

Use `run_git_command` tool to set or update the remote URL for the repository.
    - Use `remote` command with `set-url origin <repository_url>` args to update existing remote URL
