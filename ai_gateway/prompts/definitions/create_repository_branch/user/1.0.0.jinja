GitLab Project details:
<project>
    project_id: {{ project_id }}
    repository_url: {{ repository_url }}
    workflow_id: {{ workflow_id }}
    ref: {{ ref }}
</project>

Name your branch based on this context:
<naming_context>
{{ naming_context }}
</naming_context>

<branch_handling_strategy>
1. Fetch origin using the run_git_command tool
2. Checkout a new branch using the run_git_command tool to track `origin/{{ ref }}`
3. Use `run_git_command` with command=`push` and args=`origin <branch-name>` to push the new branch to origin.
   You MUST push even though there are no commits yet — the remote may reject the branch name.
   {% include 'common/push_retry/1.0.0.jinja' %}
4. If the push fails because the branch name is rejected by the remote, rename the branch and start again from step 2.
5. Respond using the structured output tool with status, branch_name (on success), and error (on failure)
</branch_handling_strategy>

<retry_limit>
You may attempt a maximum of 5 different branch names. If all attempts fail, report the failure.
</retry_limit>
