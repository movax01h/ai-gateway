---
name: Codestral 22B Code Completions
model:
  params:
    temperature: 0.32
    max_tokens: 64
unit_primitive: complete_code
prompt_template:
  user: "{{prefix}}"
params:
  timeout: 60
  stop:
    - "[PREFIX]"
    - "[MIDDLE]"
    - "[SUFFIX]"
    - "</s>[SUFFIX]"
