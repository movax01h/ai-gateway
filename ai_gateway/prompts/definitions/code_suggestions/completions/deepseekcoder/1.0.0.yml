---
name: DeepSeekCoder Code Completions
model:
  params:
    max_tokens: 128
unit_primitives:
  - complete_code
prompt_template:
  user: <｜fim▁begin｜>{{prefix}}<｜fim▁hole｜>{{suffix}}<｜fim▁end｜>
params:
  timeout: 60
