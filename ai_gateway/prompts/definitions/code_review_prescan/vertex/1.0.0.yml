name: code_review_prescan
model:
  params:
    model_class_provider: litellm
    max_tokens: 8_192
unit_primitives: []
prompt_template:
  system: |
    {% include 'code_review_prescan/system/1.0.0.jinja' %}
  user: |
    {% include 'code_review_prescan/user/1.0.0.jinja' %}
  placeholder: history
params:
  # Overriding the `AIGW_GOOGLE_CLOUD_PLATFORM__PROJECT` config, since Claude models
  # is only available in a handful of regions. See
  # https://cloud.google.com/vertex-ai/generative-ai/docs/learn/locations#genai-partner-models
  vertex_location: global
  timeout: 30
