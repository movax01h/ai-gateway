---
name: Mistral code_review_prescan
model:
  params:
    model_class_provider: litellm
    temperature: 0.1
    max_tokens: 2_000
unit_primitives:
  - review_merge_request
prompt_template:
  system: |
    {% include 'code_review_prescan/system/1.0.0.jinja' %}
  user: |
    {% include 'code_review_prescan/user/1.0.0.jinja' %}
  placeholder: history
params:
  timeout: 30
