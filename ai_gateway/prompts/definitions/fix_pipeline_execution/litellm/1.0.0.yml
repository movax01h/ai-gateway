---
name: Fix Pipeline Execution
model:
  params:
    max_retries: 3
unit_primitives:
  - duo_agent_platform
prompt_template:
  system: |
    {% include 'fix_pipeline_execution/system/1.0.0.jinja' %}
  user: |
    {% include 'fix_pipeline_execution/user/1.0.0.jinja' %}
  placeholder: history
params:
  timeout: 60

