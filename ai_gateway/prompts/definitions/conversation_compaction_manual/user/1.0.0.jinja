Summarize our conversation above into a clear, detailed summary covering what was
done, what is currently being worked on, which files are involved, what remains to
be done, and any key decisions or preferences. The user has explicitly requested
this summary; write it so they can read it directly.
{% if user_instruction %}

Additional focus requested by the user: {{ user_instruction }}
{% endif %}
