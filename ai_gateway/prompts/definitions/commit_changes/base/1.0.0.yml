name: commit_changes
model:
  params:
    model_class_provider: anthropic
    max_tokens: 32_768
    max_retries: 1
unit_primitives:
  - duo_agent_platform
prompt_template:
  system: |
    {% include 'commit_changes/system/1.0.0.jinja' %}
  user: |
    {% include 'commit_changes/user/1.0.0.jinja' %}
  placeholder: history
params:
  timeout: 30
