---
name: Claude 3 Fix Pipeline Push Changes
model:
  params:
    model_class_provider: litellm
prompt_template:
  system: |
    {% include 'fix_pipeline_push_changes/system/1.0.0.jinja' %}
  user: |
    {% include 'fix_pipeline_push_changes/user/1.0.0.jinja' %}
  placeholder: history
params:
  timeout: 60

