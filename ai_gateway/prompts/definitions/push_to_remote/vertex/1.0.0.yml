name: push_to_remote_prompt
model:
  params:
    max_tokens: 2048
    max_retries: 3
unit_primitives:
  - duo_workflow_execute_workflow
prompt_template:
  system: |
    {% include 'push_to_remote/system/1.0.0.jinja' %}
  user: |
    {% include 'push_to_remote/user/1.0.0.jinja' %}
  placeholder: history
params:
  vertex_location: global
  timeout: 30
