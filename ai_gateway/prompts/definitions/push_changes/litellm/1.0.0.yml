---
name: Push Changes
model:
  params:
    max_retries: 1
unit_primitives:
  - duo_agent_platform
prompt_template:
  system: |
    {% include 'push_changes/system/1.0.0.jinja' %}
  user: |
    {% include 'push_changes/user/1.0.0.jinja' %}
  placeholder: history
params:
  timeout: 60
