mutation CreateDiscussion($input: CreateDiscussionInput!) {
    createDiscussion(input: $input) {
        note {
            id
            body
            createdAt
            author {
                username
                name
            }
        }
        errors
    }
}
