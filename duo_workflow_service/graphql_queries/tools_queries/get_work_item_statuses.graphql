query GetWorkItemStatuses($fullPath: ID!) {
    namespace(fullPath: $fullPath) {
        lifecycles {
            nodes {
                id
                name
                statuses {
                    id
                    name
                }
            }
        }
    }
}
