"""Concrete ``HistoryOptimizer`` implementations.

Each module in this package contains a single optimizer (plus any
optimizer-internal helpers). Add new optimizers by dropping a new module
here and wiring its config into ``history_optimizer.schema`` and
``history_optimizer.builder``.
"""
